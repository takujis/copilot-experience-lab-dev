# コントリビューション ガイド

Copilot Experience Lab にコンテンツを追加・改訂するときのルールです。

---

## 大原則

1. **体験の実体は `contents/` にのみ置く。** 1 コンテンツ 1 か所。
2. **`programs/` 配下にコンテンツをコピーしない。** プログラムは相対リンク・順番・必須/選択・演出だけを持つ。
3. **ゲーム要素はプログラム側の「皮」。** ポイント、バッジ、レベル、チーム戦は `contents/` に埋め込まず、対象者に応じて `programs/` 側で選ぶ。
4. **演出は「競争」より「成長・達成・共有」を中心にする。**

---

## 体験 ID の付け方

`EXP-<領域>-<連番 3 桁>` の形式です。

| 接頭辞 | 領域 | 配置先 |
|---|---|---|
| `EXP-SETUP-` | 事前準備・セキュリティ | `contents/00-setup/` |
| `EXP-CHAT-` | Copilot Chat | `contents/01-copilot-chat/` |
| `EXP-OUT-` | Outlook | `contents/02-outlook-teams/` |
| `EXP-XLS-` | Excel | `contents/03-excel/` |
| `EXP-WRD-` | Word | `contents/04-word/` |
| `EXP-PPT-` | PowerPoint | `contents/05-powerpoint/` |
| `EXP-MTG-` | 会議シナリオ | `contents/02-outlook-teams/` |
| `EXP-CATCH-` | キャッチアップ シナリオ | `contents/02-outlook-teams/` |
| `EXP-AGT-` | エージェント | `contents/06-researcher-analyst/` または `contents/07-agent-builder/` |

一度発行した ID は変更しません（プログラムからの参照が壊れるため）。

## ファイル名の付け方

`<体験ID>_<ハンズオンの内容>.md`

例：`売上データから地域別の弱点と価格施策を導く_XLS-01.md`

- **ファイル名だけで何を体験するか分かる**ように、動詞で終わる説明的な名前にする
- 抽象的な名前（「データ分析」「文書作成」）は避け、成果が想像できる粒度にする
- 体験 ID は先頭に置き、`_`（アンダースコア）で内容と区切る
- ファイル名の内容部分は、本文の H1 タイトルおよび front matter の `title` と一致させる

---

## 体験コンテンツの必須フォーマット

```markdown
---
id: XXX-00
title: 体験のタイトル
category: apps/chat
product: 利用するサービス
level: 基礎 / 実践 / 応用
duration: 10分（目安）
data: プロンプト内完結 / サンプルファイル / 自分の業務データ
source: 元教材と該当箇所
---

# XXX-00｜タイトル

| 項目 | 内容 |
|---|---|
| **目的** | |
| **所要** | |
| **利用** | |
| **入力** | |
| **成果** | |
## シナリオ
## TRY — 手順
<!--
## WATCH
## REFLECT — 振り返り
## NEXT
```

- **TRY** のプロンプトは必ずコード ブロック（```text）に入れる。参加者がそのままコピーできるようにするため。
- **WATCH** には `contents/assets/<体験ID>/` を参照するパスを書く。
- **NEXT** は相対リンクで `contents/` 内の次の体験を指す。

---
-->

## アセットの置き方

```text
contents/assets/<体験ID>/
├─ demo.mp4
├─ demo.gif
└─ sample-output.md
```

体験 ID のフォルダーで管理し、複数プログラムから同じアセットを参照します。

---

## 新しいプログラムを追加する

1. `programs/templates/` の雛形をコピーする
2. 対象者、時間、順番、演出、ゴールだけを定義する
3. 体験は必ず `../../contents/...` への相対リンクで参照する
4. 既存の体験で足りない場合のみ、新しい体験を `contents/` に追加する

---

## 日本語化のトーン

- 翻訳調を避け、**業務担当者がそのまま読める自然な日本語**にする
- 製品名・UI ラベルは、実際の日本語 UI の表記に合わせる（表記ゆれがある場合は「表示名は環境により異なる」と補足する）
- プロンプト本文も日本語化する。ただし固有名詞（架空の企業名・製品名）は原文のまま残す
- 架空の企業・人物であることが分かるようにする

---

## レビュー観点

- [ ] 体験 ID が一意で、フォーマットに沿っている
- [ ] ファイル名から体験内容が推測でき、H1 タイトルと一致している
- [ ] 扱う製品のフォルダーに配置されている
- [ ] 目的・所要・利用・入力・成果・証跡がすべて埋まっている
- [ ] プロンプトがコピー可能な形式になっている
- [ ] `programs/` にコンテンツを複製していない
- [ ] 相対リンクが壊れていない
- [ ] 機密情報・実在の顧客情報が含まれていない
- [ ] プライバシー上の注意（自分の業務データを使う演習）が明記されている
